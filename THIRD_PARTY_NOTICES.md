# Third-Party Notices

Silo Apple is licensed under `AGPL-3.0-or-later`. Third-party dependencies keep their original licenses.

## FFmpeg

FFmpeg xcframeworks are consumed through the `mpvkit/ffmpeg-build` Swift package pinned in `iosApp/project.yml`. The project intentionally uses FFmpeg libraries without the libmpv GPL surface.

Follow upstream FFmpeg and LGPL terms when distributing Apple binaries.

## Nuke

Nuke and NukeUI are consumed as Swift package dependencies pinned in `iosApp/project.yml` and retain upstream license terms.

## fastlane

Release automation uses fastlane from RubyGems and retains upstream license terms.
