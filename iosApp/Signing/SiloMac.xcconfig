// macOS app signing defaults. CI and paid-team builds can use these as-is.
// Local contributors can override the bundle identifier by dropping a
// `Local.xcconfig` into this directory.

SILO_MAC_BUNDLE_ID = com.continuum.play.mac

PRODUCT_BUNDLE_IDENTIFIER = $(SILO_MAC_BUNDLE_ID)

#include? "Local.xcconfig"
